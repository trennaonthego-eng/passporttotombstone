/**
 * The one place the site's public contact email lives. Currently Trenna's
 * Gmail so nothing bounces; when the free domain forwarding is set up
 * (events@passporttotombstone.com → Gmail), change it here and it updates
 * site-wide.
 */
export const CONTACT_EMAIL = "trennaonthego@gmail.com";
